import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

test('Renders input and search button', () => {
  render(<App />);
});
test('Renders table', () => {
  render(<App />);
});
test('Renders filtering', () => {
  render(<App />);
});
test('Renders Error message - too short input', () => {
  render(<App />);
});
test('Renders loader', () => {
  render(<App />);
});
test('Sorts data by NIP ascending', () => {
  render(<App />);
});
test('Show correct date', () => {
  render(<App />);
});
